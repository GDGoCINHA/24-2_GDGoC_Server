package inha.gdgoc.domain.landing.enums;

/** 대회·해커톤 배지 색. 실제 색은 웹이 고른다 — 여기는 이름만 정한다. */
public enum LandingBadgeTone {
    /** 다른 챕터·학교와 함께 여는 행사. */
    UNION,
    /** 학술 행사. */
    ACADEMIC,
    /** 내부 행사. */
    INTERNAL
}
